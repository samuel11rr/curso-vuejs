// instanciamos Vue.js
const app = new Vue({
    // declaramos que la aplicación estará en el
    // elemento con el ID app
    el: '#app',
    data: {
        fondo: 'bg-warning',
        color: false
    },
    methods: {

    }
})